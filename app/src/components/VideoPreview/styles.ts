import { styled } from "styled-components"

export const VideoWrapper = styled.div`
  position: relative;
  width: 40%;

  .react-player {
    position: absolute;
    top: 0;
    left: 0;
    bottom: 0;
  }
`
